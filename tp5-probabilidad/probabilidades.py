from moneda import Moneda, cara, seca

def p_huir(epsilon, iteraciones):
    # Obtenemos la moneda
    moneda = Moneda()
    
    # Definimos la probabilidad de huir
    p = 0

    # devolvemos la probabilidad de huir
    return p
